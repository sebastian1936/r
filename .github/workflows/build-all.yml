name: all

on:
  workflow_dispatch:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-build-all.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
