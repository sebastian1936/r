name: build-windows-android

on:
  workflow_dispatch:
  push:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-build-windows-android.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
