name: build-mac

on:
  workflow_dispatch:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-build-mac.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
